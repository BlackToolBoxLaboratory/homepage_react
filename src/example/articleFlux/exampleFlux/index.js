import React, { Component } from 'react';

import App from './components/app.js';

const Index = (props) => {
  let content = [];
  content.push(
    <App/>
  );
  return content;
};

export default Index;
